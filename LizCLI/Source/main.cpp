#include "LizCLI.h"

int main(int argc, char **argv)
{
    // resolve arguments

    // load config

    // init command line
    liz::LizCLI cli;
    // check version

    // load plugin

    // main loop
    while (cli.isRunning())
    {
    }
    // clean up

    return 0;
}